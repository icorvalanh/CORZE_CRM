[phases.setup]
nixPkgs = ["pango", "cairo", "gdk-pixbuf", "libffi", "shared-mime-info", "fontconfig", "freetype"]

[start]
cmd = "gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --threads 4 --timeout 120"
